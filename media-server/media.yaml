apiVersion: v1
kind: PersistentVolume
metadata:
  name: media-volume
  namespace: media-server
spec:
  capacity:
    storage: 16Ti
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /srv/mrgfs_movies