
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: whisparr
  namespace: media-server
  annotations:
    kubernetes.io/ingress.class: traefik
    cert-manager.io/cluster-issuer: letsencrypt
    traefik.ingress.kubernetes.io/router.tls: "true"
spec:
  rules:
  - host: whisparr.wavecat.net
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: whisparr
            port:
              name: http
  tls:
  - hosts:
    - whisparr.wavecat.net
    secretName: whisparr.wavecat.net
